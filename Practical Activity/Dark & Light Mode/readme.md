# Dark and Light Mode Web Application

## Project Description
This web application demonstrates how to switch between dark and light modes using JavaScript. The project includes a simple user interface with a toggle button that allows users to switch between modes.

## Features
- Light mode and dark mode styles
- Smooth transition between modes
- Dynamic updating of button text to indicate the current mode

## Files
- `index.html`: Contains the HTML structure and content of the webpage.
- `styles.css`: Contains the CSS styles for both light and dark modes.
- `script.js`: Contains the JavaScript code for DOM manipulation and mode toggling.
- `README.md`: Provides an overview of the project and its features.

## How to Use
1. Open the `index.html` file in a web browser.
2. Click the toggle button to switch between light and dark modes.

## JavaScript Code Explanation
- The `toggleButton` variable selects the toggle button element.
- The `toggleMode` function toggles the `dark-mode` class on the `body`, `header`, and `button` elements, and updates the button text to reflect the current mode.
- An event listener is added to the toggle button to call the `toggleMode` function when the button is clicked.



Activity: Dark and Light Mode Page template
Objective: Create a web application that demonstrates DOM manipulation techniques using JavaScript by creating a templating system that changes the styles of the web page from a dark mode to a light mode based on the users interactions.

Instructions:

1. Setup the Project Structure:

Create a new folder for your project.
Inside the folder, create the following files:
index.html: This file will contain the HTML structure and content of your webpage.
styles.css: This file will contain the CSS styles for your webpage.
script.js: This file will contain the JavaScript code for DOM manipulation.
2. Create HTML Structure:

Open index.html.
Decide what type of web application you would like to build.
Some examples could include:
 # A hospital reporting system user interface.
 # A local business directory web application.
 # A blogging website web application.
 # A data analysis visualisation user interface.
 # A modern gaming website that displays gaming news articles. 
Define the basic structure and add content to your webpage.
Link the styles.css and script.js files to your HTML.
Styling:

Open styles.css.
Define styles for light mode and dark mode, ensuring proper contrast and readability.
An alternative would be to have two different color schemes, fonts or other template styles (Example a gaming page that switches from a fun pink mode to a neon cyber punk style.) 
Define all the styles for the sections of the page in the two modes.
3. Implement JavaScript for the templating system:

Open script.js.
Use what you have learnt of DOM manipulation to add various JavaScript methods and properties that are triggered by a button that will toggle between two template style modes. 
4. Testing:

Run your project in a web browser.
Test the functionality of toggling modes.
Ensure that the page looks and behaves as expected in both light and dark modes.
Use the browser's developer tools to inspect and debug your JavaScript code if necessary.
6. Documentation:

Add comments to your JavaScript code to explain the purpose of each function.
Write a brief description of your project and its features in a README.md file.

